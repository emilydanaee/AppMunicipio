@model WebMunicipio.Models.Feria

<div class="container mt-4">

    <div class="card shadow border-danger">

        <div class="card-header text-white bg-danger">
            <h3>🗑 Eliminar Feria</h3>
        </div>

        <div class="card-body">

            <h4 class="text-danger">
                ¿Estás seguro de eliminar esta feria?
            </h4>

            <hr />

            <div class="row">

                <div class="col-md-5 text-center">

                    @if (!string.IsNullOrWhiteSpace(Model.ImagenFeria))
                    {
                        <img src="https://localhost:7061/@Model.ImagenFeria"
                             class="img-fluid rounded shadow"
                             style="max-height:250px;"
                             alt="@Model.NombreFeria" />
                    }
                    else
                    {
                        <div class="alert alert-secondary">
                            Sin imagen disponible
                        </div>
                    }

                </div>

                <div class="col-md-7">

                    <p><strong>Nombre:</strong> @Model.NombreFeria</p>
                    <p><strong>Sector:</strong> @Model.SectorFeria</p>
                    <p><strong>Ubicacion:</strong> @Model.UbicacionFeria</p>
                    <p><strong>Descripción:</strong> @Model.DescripcionFeria</p>
                    <p><strong>Fechas:</strong> @Model.FechaInicio - @Model.FechaFin</p>
                    <p><strong>Horario:</strong> @Model.HoraInicio - @Model.HoraFin</p>

                </div>

            </div>

            <hr />

            <form asp-action="Delete" method="post">

                <input asp-for="IdFeria" type="hidden" />

                <button type="submit" class="btn btn-danger">
                    Confirmar Eliminación
                </button>

                <a asp-action="Index" class="btn btn-secondary">
                    Cancelar
                </a>

            </form>

        </div>

    </div>

</div>